import User from '../user';

describe('ther User -> isUserLoggedAndTokenValid', () => {
    it('token is Valid', () => {

    })

    it('token is invalid', () => {

    })

    it('token is valid and offline', () => {

    })

    it('token is invalid and online', () => {

    })
});
